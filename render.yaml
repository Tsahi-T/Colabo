services:
  - type: web
    name: colabo
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: colabo-db
          property: connectionString

databases:
  - name: colabo-db
    plan: free
